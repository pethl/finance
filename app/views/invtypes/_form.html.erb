<%= form_for(@invtype) do |f| %>
  <% if @invtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invtype.errors.count, "error") %> prohibited this invtype from being saved:</h2>

      <ul>
      <% @invtype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
